import { Button } from 'antd';
import styled from 'styled-components';

export const StyledLoginSigninBtn = styled(Button)`
  background-color: var(--primary-light-color);
  color: #fff;
  border: none;
  font-weight: 500;
`;
